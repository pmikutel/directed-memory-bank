#!/usr/bin/env node
console.log(`
Directed Memory Bank (DMB) — name reservation in progress (v0.0.1 placeholder).

The real installer ships at v0.1.0. Until then, this package only holds the
name on npm.

Track progress: https://github.com/pmikutel/directed-memory-bank
`);
process.exit(0);
