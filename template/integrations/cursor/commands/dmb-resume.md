Resume DMB context for this session.

Read these files in order:

1. `memory-bank/_adoption.md` — see what's filled, partial, postponed, or skipped.
2. `memory-bank/_index.md` — the inventory of what knowledge exists in this project.
3. `memory-bank/project/brief.md` — project overview.

If any `memory-bank/tasks/work/*.md` file has a `branch:` field matching the current git branch, also read it as live scratchpad context.

Then summarise:

- What's currently in flight (active topics in `tasks/work/`).
- The DMB sections still pending (status `partial`, `postponed`, or `not_started`).
- One concrete next thing you could help with given the current state.
