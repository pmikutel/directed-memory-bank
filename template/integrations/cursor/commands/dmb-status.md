Show DMB status for this project.

1. Read `memory-bank/_adoption.md` and list each row with its `Status` (filled / partial / postponed / skipped / not_started). One line per row.
2. List the files present under `memory-bank/tasks/work/` with their `status:` and `branch:` frontmatter, one line per file. Note "empty" if the directory has none.
3. Count entries in `memory-bank/tasks/log/` and report the most recent five filenames.

Report concisely. No prose summary — just the three lists.
